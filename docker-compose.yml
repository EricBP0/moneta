services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: moneta-backend
    env_file:
      - .env
    ports:
      - "8080:8080"
    restart: unless-stopped
